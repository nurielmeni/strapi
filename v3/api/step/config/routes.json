{
  "routes": [
    {
      "method": "GET",
      "path": "/steps",
      "handler": "step.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/steps/count",
      "handler": "step.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/steps/:id",
      "handler": "step.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/steps",
      "handler": "step.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/steps/:id",
      "handler": "step.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/steps/:id",
      "handler": "step.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
