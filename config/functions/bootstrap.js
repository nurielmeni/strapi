'use strict';

/**
 * Bootstrap is intentionally left empty.
 *
 * Group indexes are now created manually from the Admin panel using
 * POST /admin/maintenance/group-indexes.
 */
module.exports = async () => {};
